# ------------------------------------------------------------------------------
# EXAMPLES ON HOW TO WRITE PACKAGE SPECS:
# 
#
#  - pkg1
#  - pkg2@2.2
#  - pkg3:
#      variants: +spec1
#  - pkg4@1.1:
#      variants: +spec1
#  - pkg5:
#      variants:
#        common: +spec1
#  - pkg6@1.2.3:
#      variants:
#        common: +spec1
#      dependencies:
#        - dep1
#        - dep2
#  - pkg7@1.2.3:
#      dependencies:
#      - dep1
#      - dep2
#  - pkg8@1.2.3:
#      gpu:
#        nvidia: ~osmesa
#        none: +osmesa
#
# ------------------------------------------------------------------------------
# PROGRAMMING ENVIRONMENT SECTION (PE)
#
# The PE section follows the syntax: <pe_name>_<stack_name>_<package_name>. for
# example: intel_stable_compiler. There is a parent child schema that must be
# respected. The metadata key is ignored for this purpose. The core PE only has
# one information and does not follow the same schema. Any key under the
# <package_name> will be seen as a filter or it will be ignored. In the future
# we may need to introduce a stack key and a dependencies key for the case where
# we want to compile the compiler or stack libraries with GPU support. For now,
# the core_compiler should have this name (this means 'core' key and 'compiler'
# for subkey. We can also set section:core for the core PE.
core:
  metadata:
    section: core
  compiler: <core_compiler>

gcc:
  metadata:
    section: pe
  stable:
    compiler: gcc@11.3.0
    mpi:
      infiniband: openmpi@4.1.3 fabrics=ucx +pmi schedulers=slurm ~memchecker
      ethernet: openmpi@4.1.3 fabrics=ucx +pmi schedulers=slurm ~memchecker
    blas: openblas@0.3.20 threads=none +locking
    blas_parallel: openblas@0.3.20 threads=openmp +locking
    python3: python@<python3> +tkinter +optimizations ~debug
    python2: python@2.7.18 +tkinter ~debug
    gpu:
      nvidia: cuda@11.7.0
      none: ""
# CORE -------------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
core_packages:
  metadata: {section: packages }
  pe: [core]
  packages:
    - autoconf
    - automake
    - cmake:
        variants: +ncurses +ownlibs
        default:
          variants: +ownlibs ~ncurses ~qt
    - curl
    - emacs +tls
    - fastqc
    - fio
    - git
    - git-lfs
    - i7z
    - libtool
    - lmod
    - m4
    - parallel
    - perl:
        default: { version: 5.32.0 }
    - picard
    - sbt
    - sratoolkit
    - tar
    - tmux
    - trimmomatic

# CORE (STABLE COMPILER) -------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
core_packages_stable:
  metadata: {section: packages }
  pe: [gcc_stable]
  packages:
    - flex:
        default: { version: 2.6.4 }

# ------------------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages:
  metadata: { section: packages }
  pe: [gcc_stable]
  packages:
    - angsd
    - bwa
    - bzip2
    - cmake
    - eigen:
        default: { variants: +ipo }
    - fastqc
    - ffmpeg
    - fftw +openmp ~mpi
    - gsl
    - gzip
    - hdf5:
        default:
          variants: ~mpi +cxx +fortran +hl +ipo +shared +szip +tools
    - hisat2
    - htslib
    - intel-tbb
    - jasper
    - libfabric:
        default:
          variants: fabrics=tcp,udp,sockets,mlx,verbs
    - libxc
    - libxml2:
        default: { version: 2.7.8 }
    - mafft
    - metis:
        default: { variants: +real64 }
    - muscle
    # ok syntax for namd
    #- namd:
    #    variants: interface=tcl
    #    dependencies:
    #      mpi:
    #        infiniband:
    #          - charmpp ~smp backend=verbs build-target=charm++ pmi=slurmpmi2
    - perl:
        default: { version: 5.32.0 }
    - netcdf-c ~mpi ^hdf5 ~mpi +szip +hl +fortran +cxx
    - netcdf-fortran ^netcdf-c ~mpi ^hdf5 ~mpi +szip +hl +fortran +cxx
    - nfft ^fftw ~mpi
    - openssl:
        default:
          version: 1.1.1k
          buildable: False
        externals:
          - spec: openssl@1.1.1k
            prefix: /usr
    - rdma-core:
        default:
          variants: +ipo
          buildable: False
        externals:
          - spec: rdma-core@40.1
            prefix: /usr
    - scotch:
        variants: ~mpi
        default: { variants: +esmumps ~metis }
    - slurm:
        default:
          buildable: false
        externals:
          - spec: slurm@22-05-2 ~mariadb
            prefix: /usr
    - stacks
    - star
    - subread
    - tcl
    - valgrind ~boost ~mpi
    - voropp
    - xpmem:
        default:
          buildable: false
        externals:
          - spec: xpmem +kernel-module@2.6.3
            paths: /usr
    - zlib

# GCC ONLY ---------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_gcc_stable:
  metadata: { section: packages }
  pe: [gcc_stable]
  packages:
    # The installation of intel-mkl create an empty directory in the not haswell
    # environment, but one question remains : does loading the package from the
    # ivybridge environment will invoke the module from haswell ?
    - intel-mkl
    - ffmpeg +libx264
    - glpk+gmp
    # julia >= 0.5 conflicts with intel compilers
    # it is not in the serial_packages_python_gcc_stable due to the dependecy to mkl
    - julia
    - node-js
    # TODO: check if the reuse: True makes it use the proper node-js
    # node-js@18.9.1 includes npm@8.19.1
    # idea: make a new spack package with this version of npm
    # - npm
    # This is already specified in the PE section
    # Here we are just making sure it will go into packages.yaml
    - python:
        variants: +tkinter +optimizations ~debug
        default:
          version: <python3>
          variants: +tkinter +optimizations ~debug
    - sox

# GPU --------------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_gpu:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [gpu]
  packages:
    - hwloc:
        default:
          variants:
            common: ~cairo
            gpu:
              nvidia: +cuda +nvml ~rocm
              none: ~cuda ~rocm ~nvml
              amd: +rocm ~cuda
    # The following version compiler with success:
    #
    #  llvm@12.0.1%gcc@11.3.0+clang~code_signing+compiler-rt~cuda~flang+gold~internal_unwind+ipo+libcxx
    #  +link_llvm_dylib+lld+lldb+llvm_dylib~mlir+omp_as_runtime~omp_debug~omp_tsan+polly~python~shared_libs
    #  ~split_dwarf~z3 build_type=Release patches=10cb42f,88ae104,f920173 targets=amdgpu,bpf,nvptx,webassembly
    #  version_suffix=jl arch=linux-rhel8-haswell
    #
    # The following version fails:
    #
    #  llvm@14.0.2%gcc@11.3.0+clang~code_signing+compiler-rt~cuda~flang+gold+internal_unwind+ipo+libcxx
    #  ~link_llvm_dylib+lld+lldb~llvm_dylib~mlir+omp_as_runtime~omp_debug~omp_tsan+polly~python~shared_libs
    #  ~split_dwarf~z3 build_type=Release patches=d85ef51,f920173 targets=none version_suffix=none arch=linux-rhel8-haswell
    #
    - llvm:
        default:
          version: 12.0.1
          variants:
            common: +ipo
            gpu:
              nvidia: +cuda cuda_arch=<cuda_arch>
              none: ~cuda
    - ucx:
        default:
          variants:
            common: +rdmacm +rc +dc +ud +cm +cma +verbs +mlx5_dv +parameter_checking +thread_multiple
    - cudnn@8.0.3.39-11.0:
        dependencies:
          gpu:
            nvidia: cuda@11.0.2

# GPU (GCC ONLY) ---------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_gcc_stable_gpu:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [gpu]
  packages:
    - xgboost:
        variants:
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch>
            none: ~cuda

# BLAS -------------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_blas:
  metadata:
    section: packages
  pe:
    - gcc_stable
  dependencies:
    - blas
  packages:
    - arpack-ng ~mpi
    # bzsotdv ^hdf5@1.10.7 +cxx+fortran+hl+ipo~java~mpi+shared+szip~threadsafe+tools api=default build_type=RelWithDebInfo patches=2a1e311 arch=linux-rhel8-haswell
    # +med ^hdf5@1.10.7
    - gmsh:
        variants: |
          ~mpi +hdf5 +cgns +eigen ~opencascade +openmp ~fltk
        dependencies:
          - scotch ~mpi
          - cgns ~mpi
          - hdf5 ~mpi
          - med ~mpi
          - mmg
    # xkqh54e
    - ncview ^netcdf-c~mpi ^hdf5~mpi ^boost@1.79.0+atomic+chrono~clanglibcpp+container~context~contract~coroutine+date_time~debug+exception~fiber+filesystem+graph~graph_parallel~icu+iostreams~json+locale+log+math~mpi+multithreaded~nowide+numpy~pic+program_options+python+random+regex+serialization+shared+signals~singlethreaded~stacktrace+system~taggedlayout+test+thread+timer~type_erasure~versionedlayout+wave cxxstd=98 patches=a440f96,b8569d7 visibility=hidden
    - superlu

# BLAS + GPU -------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_blas_gpu:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [blas, gpu]
  packages:
    - suite-sparse:
        variants:
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch>
            none: ~cuda
    - magma:
        variants:
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch>

# BLAS (GCC ONLY) --------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_blas_gcc_stable:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [blas]
  packages:
    - armadillo +hdf5 ^arpack-ng ~mpi ^hdf5 ~mpi
    - octave

# MPI --------------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
mpi_packages:
  metadata:
    section: packages
  pe:
    - gcc_stable
  dependencies:
    - mpi
  packages:
    - arpack-ng:
        default: { variants: +mpi }
    - fftw:
        variants: +mpi +openmp
        default: { variants: +mpi ~openmp }
    - hdf5 +mpi +szip +hl +fortran +cxx
    - netcdf-c +mpi ^hdf5 +mpi +szip +hl +fortran +cxx
    - netcdf-fortran ^hdf5 +mpi +szip +hl +fortran +cxx
    - openfoam-org +metis ^scotch+mpi
    - parmetis:
        default: { variants: +ipo }
    - phylobayesmpi
    - scotch +mpi

# MPI + BLAS -------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
mpi_blas_packages_gcc_stable:
  metadata:
    section: packages
  pe:
    - gcc_stable
  dependencies:
    - mpi
    - blas
  packages:
    - abinit +openmp +mpi +scalapack +wannier90
    - elmerfem +mumps +openmp +hypre:
        dependencies:
          - hdf5+cxx+fortran+hl+ipo~java+mpi+shared+szip~threadsafe+tools api=default build_type=RelWithDebInfo
          - netcdf-fortran@4.5.4%gcc@11.3.0~doc+pic+shared
    - gromacs@2021.4+mpi:
        variants:
          gpu:
            nvidia: +cuda
            none: ~cuda
        dependencies:
          - fftw~mpi+openmp
	  - openmpi@4.1.3%gcc@11.3.0~atomics~cuda~cxx~cxx_exceptions~gpfs~internal-hwloc~java~legacylaunchers~lustre~memchecker+pmi+romio+rsh~singularity+static+vt+wrapper-rpath fabrics=ucx schedulers=slurm
    # These dependencies (of mumps) will not go into packages.yaml
    - mumps:
        default: { variants: +mpi +parmetis +metis +scotch +ptscotch }
        dependencies:
          - scotch +mpi
          - netlib-scalapack
    - netlib-scalapack:
        default: { variants: +ipo }
    - yambo+mpi io=iotk,etsf-io

# MPI + BLAS ------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
mpi_lapack_packages:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [mpi, blas]
  packages:
    - gmsh +openmp +hdf5 ~fltk ~opencascade
    - quantum-espresso+mpi+scalapack
    - quantum-espresso+mpi+scalapack hdf5=parallel

# MPI + BLAS + GPU -------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
mpi_lapack_gpu_packages:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [gpu, mpi, blas]
  packages:
    - hypre:
        default:
          variants:
            gpu:
              nvidia: +cuda
              none: ~cuda
    - superlu-dist:
        default:
          variants:
            common: +ipo
            gpu:
              nvidia: +cuda
              none: ~cuda

# PYTHON ACTIVATED -------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_python_activated:
  metadata:
    section: packages
    modules:
      blacklist: True
    activated: True
  pe: [gcc_stable]
  dependencies: [python3]
  packages:
    - py-absl-py
    - py-astunparse
    - py-cython
    - py-gast
    - py-google-pasta
    - py-grpcio
    - py-pip
    - py-ply
    - py-protobuf
    - py-requests
    - py-semver
    - py-six
    - py-sympy
    - py-termcolor
    - py-virtualenv
    - py-wheel
    - py-wrapt

# PYTHON ACTIVATED - BLAS  -----------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_python_blas_activated:
  metadata:
    section: packages
    modules: { blacklist: True }
    activated: True
  pe: [gcc_stable]
  dependencies: [python3, blas_parallel]
  packages:
    - py-keras-preprocessing
    - py-matplotlib
    - py-numpy
    - py-opt-einsum
    - py-pandas
    - py-scipy
    - py-xarray

# PYTHON -----------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_python:
  metadata:
    section: packages
  pe: [gcc_stable]
  dependencies: [python3]
  packages:
    - bedtools2
    - bowtie2
    - cairo:
        default: { variants: +png +pdf +fc +ft }
    - mercurial
    - ninja
    - prinseq-lite
    - py-h5py ^hdf5:
        modules:
          autoload: direct
    - py-pybind11
    - samtools
    - scons
    - snakemake

# PYTHON BLAS ------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_python_blas:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [python3, blas_parallel]
  packages:
    - boost:
        variants: ~mpi
        default: { variants: +atomic +chrono +container +date_time +exception +filesystem +graph +icu +iostreams +locale +log +math +multithread +numpy +program_options +python +random +regex +serialization +shared +signals +system +test +thread +timer +wave }
    - iq-tree +ipo@1.6.12
    - iq-tree +ipo@2.0.6
    - py-biopython
    - py-deeptools
    - py-keras ^py-h5py ^hdf5 ~mpi
    - py-macs2
    - py-pybigwig
    - py-scikit-learn
    - py-statsmodels

# PYTHON BLAS GPU --------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_python_blas_gpu:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [blas_parallel, python3, gpu]
  packages:
    - py-theano:
        variants:
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch>
            none: ~cuda

# PYTHON 2 ---------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_python2_deprecated:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [python2]
  packages:
    - jellyfish
    - scons
    - py-pip
    - py-virtualenv

# PYTHON - GCC ONLY ------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_python_gcc_stable:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [python3]
  packages:
    # needs boost@1.54 (https://backwardincompatible.com/post/169360794395/compiling-illumina-bcl2fastq-220-on-ubuntu-with)
    - bcl2fastq2
    - blast-plus@2.9.0
    - gdb +tui +source-highlight +lto +xz ^boost@1.79.0+atomic+chrono~clanglibcpp+container~context~contract~coroutine+date_time~debug+exception~fiber+filesystem+graph~graph_parallel~icu+iostreams~json+locale+log+math~mpi+multithreaded~nowide+numpy~pic+program_options+python+random+regex+serialization+shared+signals~singlethreaded~stacktrace+system~taggedlayout+test+thread+timer~type_erasure~versionedlayout+wave cxxstd=98 patches=a440f96,b8569d7 visibility=hidden
    - libgd
    - mummer
    - polymake@3.5
    - openbabel +python
    - r:
        default: {variants: +external-lapack}
    - gatk
    - spades

# PYTHON + GPU (GCC ONLY) ------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
serial_packages_python_gpu_gcc_stable:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [python3, gpu]
  packages:
    - caffe:
        variants:
          common: ~opencv +python
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch>
            none: ~cuda
        dependencies:
          - boost@1.79.0+atomic+chrono~clanglibcpp+container~context~contract~coroutine+date_time~debug+exception~fiber+filesystem+graph~graph_parallel~icu+iostreams~json+locale+log+math~mpi+multithreaded~nowide+numpy~pic+program_options+python+random+regex+serialization+shared+signals~singlethreaded~stacktrace+system~taggedlayout+test+thread+timer~type_erasure~versionedlayout+wave cxxstd=98 patches=a440f96,b8569d7 visibility=hidden
    - vtk +ipo+ffmpeg~mpi+opengl2+python~qt+xdmf:
        variants:
          gpu:
            nvidia: ~osmesa
            none: +osmesa
        dependencies:
          - boost@1.79.0+atomic+chrono~clanglibcpp+container~context~contract~coroutine+date_time~debug+exception~fiber+filesystem+graph~graph_parallel~icu+iostreams~json+locale+log+math~mpi+multithreaded~nowide+numpy~pic+program_options+python+random+regex+serialization+shared+signals~singlethreaded~stacktrace+system~taggedlayout+test+thread+timer~type_erasure~versionedlayout+wave cxxstd=98 patches=a440f96,b8569d7 visibility=hidden
# EXTERNAL ---------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
external_packages:
  metadata:
    section: packages
    cloud: False
  pe: [core]
  packages:
    - openjdk:
        default:
          version: <jdk_version>
        externals:
          - spec: openjdk@<jdk_version>
            prefix: /usr/lib/jvm/java-1.8.0-openjdk-1.8.0.332.b09-1.el8_5.x86_64/jre
        modules:
          blacklist: True
    - adf@2017.111
    - ansys@19.2
    - ansys@2022R1
    - cfdplusplus@19.1
    - comsol@5.6
    - comsol@6.0
    - fdtd@2022-R1.1-2963
    - gaussian@g16-C.01
    - gurobi@9.5.2
    - likwid@4.3.0
    - maple@2017
    - mathematica@13.0
    - matlab@R2019b
    - smr@2017.06
    - totalview@2020.3.11

# MPI PYTHON --------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
mpi_python_packages:
  metadata:
    section: packages
  pe: [gcc_stable]
  dependencies: [mpi, python3]
  packages:
    - py-h5py ^hdf5 +mpi
    - py-mpi4py

# MPI + BLAS + PYTHON ----------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
mpi_lapack_python_packages:
  metadata:
    section: packages
  pe:
    - gcc_stable
  dependencies:
    - mpi
    - blas
    - python3
    - gpu
  packages:
    - boost +mpi
    - cgal ^boost+mpi
    - iq-tree@1.6.12+mpi ^boost +mpi
    - neuron +mpi +python
    - plumed:
        default: { variants: +mpi +gsl }
    - petsc:
        default:
          variants:
            common:  ~int64 +double +hdf5 +metis +mpi +superlu-dist +hypre +suite-sparse
            gpu:
              nvidia: +cuda
              none: ~cuda
        dependencies:
          - hdf5 +mpi +szip +hl +fortran +cxx
    - slepc:
        default: { variants: +arpack }
    - py-petsc4py
    - py-torch:
        variants: +mpi
        default:
          variants:
            gpu:
              nvidia: +cuda cuda_arch=<cuda_arch> +magma
              none: ~cuda ~cudnn ~nccl
              rocm: +magma +rocm
        dependencies:
          gpu:
            nvidia: [magma +cuda cuda_arch=<cuda_arch>]
            rocm: [magma +rocm amdgpuarch=<amd_arch>]
    - py-torchvision:
        dependencies:
          - py-torch+mpi~cuda

# MPI (GCC ONLY) ---------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
mpi_packages_gcc_stable:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [mpi]
  packages:
    # cpmd fails to fetch
    # - cpmd~openmp ^fftw+mpi~openmp
    - relion ~mklfft:
        variants:
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch> +double-gpu
            none: ~cuda

# MPI BLAS PYTHON (GCC ONLY) ---------------------------------------------------
#
#
# ------------------------------------------------------------------------------
mpi_blas_python_packages_gcc_stable:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [mpi, blas, python3]
  packages:
    - adios2:
        default: { variants: +hdf5 +mpi +python }
    # Cannot fetch
    # - amber +mpi:
    #     variants:
    #       gpu:
    #         nvidia: +cuda cuda_arch=<cuda_arch>
    #         none: ~cuda
    #     dependencies:
    #       - boost +mpi
    - cp2k +mpi ~plumed smm=blas:
        variants:
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch>
            none: ~cuda
        dependencies:
          - boost +mpi
    - cp2k +mpi +plumed smm=blas:
        variants:
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch>
            none: ~cuda
        dependencies:
          - boost +mpi
    - fenics +parmetis +hdf5 +scotch +suite-sparse ~vtk ~trilinos ^boost +mpi
    - gromacs@2021.4+mpi+plumed:
        variants:
          gpu:
            nvidia: +cuda
            none: ~cuda
        dependencies:
          - fftw~mpi+openmp
    - lammps@20220107:
        variants:
          common: |
            build_type=Release +asphere +atc +body +class2 +colloid +compress
            +coreshell +dipole +diffraction +extra-dump +granular +h5md +kspace
            +latboltz +latte +lib +manybody +mc +misc +molecule +mpi +mpiio
            +netcdf +peri ~poems +python +qeq +replica +rigid +shock +ml-snap
            +srd +voronoi +plumed
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch>
            none: ~cuda
        dependencies:
          - netcdf-c+mpi
    - openfoam +metis ^boost +mpi ^hdf5+cxx+fortran+hl+ipo~java+mpi+shared+szip~threadsafe+tools api=default build_type=RelWithDebInfo
    - paraview +shared +python3 +hdf5:
        variants:
          gpu:
            nvidia: ~osmesa
            rocm: ~osmesa
            none: +osmesa
        dependencies:
          - netcdf-c+mpi
    - py-horovod controllers=mpi frameworks=pytorch,tensorflow,keras:
        variants:
          gpu:
            nvidia: tensor_ops=nccl +cuda cuda_arch=<cuda_arch>
            none: tensor_ops=mpi ~cuda
    - py-tensorflow:
        default:
          variants:
            common: +mpi
            gpu:
              nvidia: +cuda cuda_arch=<cuda_arch>
              none: ~cuda ~nccl
        dependencies:
          - bazel
    - opencv +vtk +python3:
        variants:
          gpu:
            nvidia: +cuda cuda_arch=<cuda_arch>
            none: ~cuda
        dependencies:
          - netcdf-c +mpi
          - boost +mpi
    - vtk+ffmpeg+mpi+opengl2+python~qt+xdmf:
        variants:
          gpu:
            nvidia: ~osmesa
            none: +osmesa
        dependencies:
          - netcdf-c +mpi
          - boost +mpi

# BENCHMARKS -------------------------------------------------------------------
#
#
# ------------------------------------------------------------------------------
benchmarks:
  metadata: { section: packages }
  pe: [gcc_stable]
  packages:
    - stream
    - stream+openmp

mpi_benchmarks:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [mpi]
  packages:
    - osu-micro-benchmarks
    - hp2p

mpi_blas_benchmarks:
  metadata: { section: packages }
  pe: [gcc_stable]
  dependencies: [mpi, blas]
  packages:
    - hpl~openmp
