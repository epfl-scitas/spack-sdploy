modules:
  default:
    roots:
      lmod: {{ data['lmod_roots'] }}
      tcl: {{ data['tcl_roots'] }}
    enable:
    - lmod
    - tcl
    lmod:
      hash_length: 0
      core_compilers:
      - {{ data['core_compiler'] }}
      hierarchy:
      - mpi
      blacklist_implicits: true
      all:
        suffixes:
{% for variant, suffix in data['suffixes'].items() %}
          {{ variant }}: {{ suffix }}
{% endfor %}
